-- CREATE TABLE inputs (
--     id INT PRIMARY KEY,
--     search TEXT
--     );
-- INSERT INTO inputs (id,search)
-- VALUES (2,'Lex Fridman Podcast ');
-- behave -f allure_behave.formatter:AllureFormatter -o reports/ features/youtube.feature
-- allure generate --clean reports/
-- allure open
SELECT * FROM inputs